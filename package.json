{
  "name": "myapp",
  "description": "a really cool app",
  "version": "1.0.0",
  "engines": {
    "node": "10.3.0"
  },
  "scripts": {
	  "start": "(node ./ui/node_modules/approuter/approuter.js) && (node ./js/server.js)",
	  "heroku-postbuild": "(cd js && npm install && npm run build) && (cd ui && npm install && npm run build)"
  }
}
